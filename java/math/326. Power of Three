class Solution {
    public boolean isPowerOfThree(int n) {
        
        // zero and negative number is false
        if(n>1) {
            while(n%3==0) n /= 3; // need to use % becuse 5/3 = 1
        }
        return n == 1;
    }
}
